{
  "name": "@mekik/workspace",
  "private": true,
  "type": "module",
  "description": "mekik - the realtime serving layer for ilmek graphs. TypeScript workspace root.",
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean",
    "typecheck": "tsc -b",
    "test:types": "pnpm -r --filter \"./packages/**\" typecheck",
    "test": "pnpm -r --filter \"./packages/**\" test",
    "test:core": "node --test packages/core/test/*.test.ts",
    "check": "pnpm build && pnpm test:types && pnpm test && node examples/refund.ts && node examples/sql-agent.ts --probe && node examples/weather-agent.ts --probe && node examples/concierge.ts --probe && node examples/routed-desk.ts --probe"
  },
  "devDependencies": {
    "typescript": "^5.9.0",
    "@types/node": "^24.0.0"
  },
  "license": "MIT"
}
